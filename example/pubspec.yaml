name: abdalsalam_logic_flutter_example
description: "Example app demonstrating app state detection features."
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.10.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  abdalsalam_logic_flutter:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
